// @req SCD-DET-001
/** ISR interval for live API fetches (seconds). Page segment uses literal `300` in app/requirements/[id]/page.tsx — Next.js requires a literal there. */
export const DATA_REVALIDATE_SECONDS = 300;
