export default function Home() {
  return (
    <main style={{ padding: '2rem' }}>
      <h1>SDD Navigator Dashboard</h1>
      <p style={{ color: 'var(--muted)', marginTop: '0.5rem' }}>
        Scaffold ready — UI implementation follows in later steps.
      </p>
    </main>
  );
}
